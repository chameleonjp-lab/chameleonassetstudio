# Chameleon Asset Studio App — Feature Requirements Index（完成系逆算・機能インベントリ）

状態: **draft / concept only / living document**（詳細化フェーズ中は継続更新する）
最終更新日: 2026-07-25
対象リポジトリ: `chameleonjp-lab/chameleonassetstudio`
文書種別: 将来コンセプト（機能インベントリ／機能仕様の入口）
入口文書: `docs/future/app/README.md`
中枢: `docs/future/app/APP_RELATIONSHIP_MAP.md`（関係マップ）

---

> **この文書の役割**
>
> 粗い大枠（Vision / Architecture / Roadmap）を、**完成系から逆算して 1 機能ずつ具体化する**ための入口。ここでは (1) 完成系の定義、(2) そこから逆算した**機能インベントリ（全機能の一覧）**、(3) 各機能を同じ形で書くための**仕様テンプレート**、(4) 詳細化の推奨順を持つ。
>
> 個々の機能仕様は `features/F-<領域>-<番号>-<名前>.md` に 1 機能 1 ファイルで置く。詳細化したら関係マップ（`APP_RELATIONSHIP_MAP.md`）を更新する（living document 契約）。
>
> **docs-only / concept only。実装・dependency・schema・価格は確定しない。**

---

## 1. 逆算の方法（完成系 → 機能 → 仕様・要件）

1. **完成系を言い切る**: 「利用者が最終的に何を、迷わず、確実にできる状態か」を、エディションごとに具体文で固定する（2 章）。
2. **各完成系文から機能を導く**: 「これが成り立つには何の機能が要るか」を問い、機能を洗い出す（3 章のインベントリ）。
3. **機能ごとに仕様・要件を書く**: 4 章のテンプレートで、機能要件（FR）・非機能要件（NFR）・データ影響・依存・受け入れ条件・非目標・関係マップ linkage を埋める。
4. **上流から詰める**: 依存の土台（Core / 保存 / モジュール機構）を先に、次に各エディションの制作パイプラインを段の順に詳細化する（5 章）。
5. **地図を更新する**: 機能を詳細化するたびに関係マップのノード粒度・エッジ・カバレッジを更新し、抜けを可視化し続ける。

---

## 2. 完成系の定義（逆算の起点）

上位は `APP_PRODUCT_VISION.md` 4・8 章。ここでは逆算に使える粒度で「done」を言い切る。すべて**オフライン・軽量ネイティブ・非破壊・必要な機能だけ導入**が前提。

### 2.1 2D Edition の完成系

利用者は、軽量なネイティブアプリで、次を迷わず・確実にできる。

- **D-DONE-1**: 空キャンバス／テンプレート／画像取り込み／図形／パーツから 2D アセットを新規作成できる。
- **D-DONE-2**: レイヤー・パーツ・フレームを整理し、ラスタ／ベクタを非破壊で編集・修正できる。
- **D-DONE-3**: 原点・アンカー・当たり判定（rect / circle / polygon）を付けられる。
- **D-DONE-4**: フレームアニメーションと簡易リグ焼き込みを扱える。
- **D-DONE-5**: tileset / sprite sheet を再整理し、atlas に詰められる。
- **D-DONE-6**: 「エンジンで使える状態か」を検品できる（命名・サイズ・原点・atlas 整合）。
- **D-DONE-7**: PNG / WebP / Sprite Sheet / Atlas / JSON / ZIP を、対象別 preset と import notes つきで書き出せる。
- **D-DONE-8**: プロジェクトを保存し、開き直して**完全に再開**でき、クラッシュしても復旧できる。

### 2.2 3D Edition の完成系

- **T-DONE-1**: GLB / glTF（+bin+textures）を**安全に**読み込める（壊れ・危険を検知）。
- **T-DONE-2**: よくある他形式（OBJ 等）も取り込める。
- **T-DONE-3**: ファイルサイズ・ポリゴン数・マテリアル・テクスチャ・bounds・妥当性・VRM を検品できる。
- **T-DONE-4**: 原点・足元・向き・スケールを整え、3D アンカーと 3D 当たり判定（box / sphere / capsule）を付けられる。
- **T-DONE-5**: 骨格を検査し、「右手・頭」等の対応付けができる。
  - ⚠️ **範囲の未決あり（`APP-O-04`）**: 上流 `docs/future/3d/README.md` は 2026-07-20 の第2改訂で「骨入れは外部ツールの仕事」という方針を改め、**骨組みの当てはめ・肌の自動追従・動きの付与／流用までツール内で完了する**計画へ変更した。本コンセプトの `T-DONE-5` は現在**検査と対応付けまで**であり、付与は含まない。**取り込むかは人間判断**（Vision 7 章の非目標との線引きを引き直す必要がある）。
- **T-DONE-6**: **元を壊さず**軽量化した別モデルを作り、前後を比較して選べる。
- **T-DONE-7**: metadata と inspection report を付けられる。
- **T-DONE-8**: GLB + metadata + report + README + import notes の ZIP を、対象別 preset つきで書き出せる。
- **T-DONE-9**: 保存・再開でき、**元の 3D モデルを絶対に上書きしない**。

### 2.3 共有の完成系

- **S-DONE-1**: Home / Project Dashboard から 2D・3D いずれのプロジェクトへも入れる。
- **S-DONE-2**: 最小構成で軽く起動し、**必要な機能だけをインストール**して広げられる。使わない機能の重さを負わない。
- **S-DONE-3**: 2D と 3D が、別最適化されつつ同じ操作感で行き来できる。
- **S-DONE-4**: 失敗・制限が文章で分かり、手動で必ず回復できる。
- **S-DONE-5**: どのモジュール構成でも同じプロジェクトを開け、未知データを壊さない。

---

## 3. 機能インベントリ（逆算で洗い出した全機能）

機能 ID: `F-<領域>-<番号>`。領域: `CORE`（土台）/ `MOD`（モジュール機構）/ `PLT`（プラットフォーム・性能）/ `2D` / `3D` / `UX`。
仕様状態: 未 = 未着手 / 粗 = 概要のみ / 詳 = 仕様確定（`features/` に個別ファイル）。

### 3.1 Core / 共有土台

| 機能 | 名称 | 由来（完成系） | 状態 |
|---|---|---|---|
| F-CORE-01 | Project & Document ライフサイクル（新規・開く・保存・再開） | D-DONE-8, T-DONE-9, S-DONE-1 | **詳** |
| F-CORE-02 | 自動保存 & クラッシュ復旧 | D-DONE-8 | **詳** |
| F-CORE-03 | 非破壊編集モデル（source 保持・派生分離） | D-DONE-2, T-DONE-6/9 | **詳** |
| F-CORE-04 | Undo / Redo & 履歴 | D-DONE-2 | **詳** |
| F-CORE-05 | 保存形式・バージョニング・migration | D-DONE-8, S-DONE-5 | **詳** |
| F-CORE-06 | 検品 / Inspection フレーム | D-DONE-6, T-DONE-3 | **詳** |
| F-CORE-07 | 書き出しフレーム（ZIP + README + import notes の器） | D-DONE-7, T-DONE-8 | **詳** |
| F-CORE-08 | Home / Project Dashboard | S-DONE-1 | **詳** |
| F-CORE-09 | Extension Host & モジュール・ライフサイクル | S-DONE-2 | **詳** |
| F-CORE-10 | capability / 権限の強制 | S-DONE-2 | **詳** |

### 3.2 モジュール機構

| 機能 | 名称 | 由来 | 状態 |
|---|---|---|---|
| F-MOD-01 | モジュールカタログ & 発見 | S-DONE-2 | **詳** |
| F-MOD-02 | インストール / アンインストール（可逆・コスト戻り） | S-DONE-2 | **詳** |
| F-MOD-03 | 依存解決 & footprint 開示 | S-DONE-2 | **詳** |
| F-MOD-04 | ライセンス検証ゲート | S-DONE-2 | **詳** |
| F-MOD-05 | プロファイル（推奨セット束ね） | S-DONE-2 | **詳** |
| F-MOD-06 | 文脈連動の「この機能が要る」提示 | S-DONE-2, S-DONE-4 | **詳** |

### 3.3 プラットフォーム・性能

| 機能 | 名称 | 由来 | 状態 |
|---|---|---|---|
| F-PLT-01 | ネイティブ外殻 & ウィンドウ / OS 連携 | S-DONE-2 | **詳** |
| F-PLT-02 | ネイティブ ファイル I/O & ダイアログ | D-DONE-7, T-DONE-1 | **詳** |
| F-PLT-03 | 重い処理の offload（worker / native / GPU） | T-DONE-6, S-DONE-2 | **詳** |
| F-PLT-04 | 性能予算の計測 & 退行検知 | S-DONE-2 | **詳** |
| F-PLT-05 | エディション別レンダラのロード | S-DONE-3 | **詳** |

### 3.4 2D Edition

| 機能 | 名称 | 由来 | 状態 |
|---|---|---|---|
| F-2D-01 | 新規作成（空 / テンプレート / 画像 / 図形 / パーツ） | D-DONE-1 | **詳** |
| F-2D-02 | 画像取り込み & 修復 | D-DONE-1 | **詳** |
| F-2D-03 | Canvas & ビューポート（2D） | D-DONE-2 | **詳** |
| F-2D-04 | レイヤー & パーツ | D-DONE-2 | **詳** |
| F-2D-05 | ラスタ編集ツール | D-DONE-2 | **詳** |
| F-2D-06 | ベクタ / 図形 | D-DONE-1/2 | **詳** |
| F-2D-07 | フレーム & フレームアニメーション | D-DONE-4 | **詳** |
| F-2D-08 | 簡易リグ焼き込み | D-DONE-4 | **詳** |
| F-2D-09 | 原点 / アンカー付与（2D） | D-DONE-3 | **詳** |
| F-2D-10 | 当たり判定付与（2D: rect / circle / polygon） | D-DONE-3 | **詳** |
| F-2D-11 | tileset / sprite sheet 整理 | D-DONE-5 | **詳** |
| F-2D-12 | Atlas パッキング | D-DONE-5 | **詳** |
| F-2D-13 | パレット / ディザ（モジュール） | D-DONE-2 | **詳** |
| F-2D-14 | バッチ & バリアント | D-DONE-5 | **詳** |
| F-2D-15 | 2D 検品（エンジン適合チェック） | D-DONE-6 | **詳** |
| F-2D-16 | 2D 書き出し preset & import notes | D-DONE-7 | **詳** |

### 3.5 3D Edition

| 機能 | 名称 | 由来 | 状態 |
|---|---|---|---|
| F-3D-01 | GLB / glTF 安全読み込み | T-DONE-1 | **詳** |
| F-3D-02 | 多形式取り込み（OBJ 等） | T-DONE-2 | **詳** |
| F-3D-03 | 3D ビューポート & 検品表示 | T-DONE-3 | **詳** |
| F-3D-04 | scene graph / node / mesh / material 検査 | T-DONE-3 | **詳** |
| F-3D-05 | 原点 / 足元 / 向き / スケール整備 | T-DONE-4 | **詳** |
| F-3D-06 | 3D アンカー付与 | T-DONE-4 | **詳** |
| F-3D-07 | 3D 当たり判定付与（box / sphere / capsule） | T-DONE-4 | **詳** |
| F-3D-08 | 骨格検査 & 人型対応付け | T-DONE-5 | **詳** |
| F-3D-09 | 人型アバター形式の検出 & 利用条件表示 | T-DONE-3 | **詳** |
| F-3D-10 | 軽量化: mesh 簡略化（非破壊の別モデル） | T-DONE-6 | **詳** |
| F-3D-11 | 軽量化: テクスチャ圧縮 | T-DONE-6 | **詳** |
| F-3D-12 | 前後比較 | T-DONE-6 | **詳** |
| F-3D-13 | 3D ゲームデータ + metadata | T-DONE-7 | **詳** |
| F-3D-14 | 3D 検品 / inspection report | T-DONE-3/7 | **詳** |
| F-3D-15 | 3D 書き出し（GLB+metadata+report+notes）preset | T-DONE-8 | **詳** |
| F-3D-16 | 骨組みの当てはめ（骨格フィット） | T-DONE-5（**範囲未決 `APP-O-04`**） | 保留 |
| F-3D-17 | 肌の自動追従（自動ウェイト） | T-DONE-5（**範囲未決 `APP-O-04`**） | 保留 |
| F-3D-18 | 動きの付与・流用（テンプレート / retarget） | T-DONE-5（**範囲未決 `APP-O-04`**） | 保留 |

### 3.6 UX / 横断

| 機能 | 名称 | 由来 | 状態 |
|---|---|---|---|
| F-UX-01 | 一貫デザインシステム（2D/3D 共通） | S-DONE-3 | **詳** |
| F-UX-02 | アクセシビリティ（role / name / キーボード） | S-DONE-4 | **詳** |
| F-UX-03 | エラー・制限メッセージ（文章で分かる） | S-DONE-4 | **詳** |
| F-UX-04 | 対象端末の方針 | S-DONE-3 | **詳** |
| F-UX-05 | 初回起動・最小オンボーディング | S-DONE-2 | **詳** |

> 合計 53 機能（初版）。洗い出しは網羅を優先し、実装可否・優先度は各機能仕様と Roadmap の Stage で判断する。新しい機能が見つかったら追加し、由来（完成系）を必ず紐づける。

---

## 4. 機能仕様テンプレート（1 機能 1 ファイル）

各 `features/F-*.md` は次を埋める。

```md
# F-<領域>-<番号> <名称>

- 状態 / エディション / 所属パイプライン段
- 完成系での位置づけ（由来する D-DONE / T-DONE / S-DONE を明記＝逆算リンク）
- 対象モジュール / Core との関係（関係マップのノード ID）

## ユーザーストーリー
- 誰が・何を・なぜ

## 機能要件（FR）
- FR-1 …（番号付きで、検証可能な粒度）

## 非機能要件（NFR）
- 性能 / 信頼性（非破壊・データ非損失）/ オフライン / アクセシビリティ / 軽量

## データ・保存への影響
- schema 接点、不変条件、UI 一時状態を保存に混ぜない境界

## 依存機能
- dependsOn: F-…（先に要る機能）

## 受け入れ条件（Acceptance）
- 「こうなれば done」の観測可能な条件

## 非目標
- この機能でやらないこと

## 関係マップ linkage
- touch する node / edge（`APP_RELATIONSHIP_MAP.md` へ昇格登録）

## Open questions
- 未決事項（推測で埋めない）
```

---

## 5. 詳細化の推奨順（上流の土台から）

依存の土台を先に固めると、後続機能の仕様が安定する。

1. **土台**: F-CORE-01（済）→ F-CORE-05（保存形式）→ F-CORE-03（非破壊）→ F-CORE-04（Undo/Redo）→ F-CORE-09/10（Host・権限）。
2. **モジュール機構**: F-MOD-01〜04（カタログ・導入・依存・ライセンス）。install-on-demand の中核。
3. **性能・外殻**: F-PLT-01〜05。軽量前提を計測で裏づける。
4. **共通フレーム**: F-CORE-06（検品）・F-CORE-07（書き出し）・F-CORE-08（Dashboard）。
5. **2D パイプライン**: F-2D-01 → 04 → 09/10 → 07/08 → 11/12 → 15/16 の順（作る→整える→検品→書き出す）。
6. **3D パイプライン**: F-3D-01 → 03 → 05 → 06/07 → 10/11/12 → 14/15。
7. **UX 横断**: F-UX-01〜05 は各段と並走。

> 順序は目安。関係マップのエッジ（依存）と Roadmap の Stage に整合させる。ユーザーの指示で順を変えてよい。

---

## 6. 系譜マップ（上流知見 → 本コンセプトの機能）

現行 Chameleon（ブラウザ版）で人間承認済みの ADR と実測 docs は、本コンセプトにとって **再導出しなくてよい知見**である。ここでは「どの上流を、どの機能の詳細化で参照するか」を一覧にし、逆算のたびに探し直さないようにする。

> **扱いの原則**: 上流は **知見（precedent）として継承**し、**制約としては継承しない**（`APP_PRODUCT_VISION.md` 6 章）。本コンセプトは別系統であり、上流の実装・schema をそのまま持ち込む決定はしていない。

| 上流（正本） | 継承する知見 | 活かす機能 | 状態 |
|---|---|---|---|
| `adr/0007` データ層分離 | UI state / 保存データ / export の責務分離 | F-CORE-01, F-CORE-03 | 反映済 |
| **`adr/0001` 座標・transform 意味論** | **共通アセット座標（左上原点・x+右・y+下・px・度）／grid・snap は UI 補助で保存座標の意味を変えない・開いただけで丸めない／出力先の Y 軸・pivot 差はキャンバスで吸収せず書き出し側が担う** | **F-2D-03 Canvas、F-2D-09/10、F-2D-16** | **反映済（本更新）** |
| **`adr/0003` variant と派生解釈** | **1 元から独立した複数派生／派生関係の正本は 1 か所（optional・additive）に置きアセット本体へ重複させない・field 不在なら全て独立／単なる複製・反転コピーを自動で関係登録しない／1 アセットは高々 1 関係 1 役割／自動一括更新を行わず base 保存だけでは variant を更新しない（明示 refresh まで上書きしない）／linked は明示 write-set・内部要素別 ID 対応・最後に同期した fingerprint を必須／再生成できる種類（linked）と記録だけの種類（manual）を分ける／出力倍率違いを variant として複製しない／base 削除は付け替えか関係解除まで拒否し、解除はメンバーを削除せず単独へ戻す／関係 metadata は再編集用の保存にだけ含めゲーム向け出力へ入れない** | **F-CORE-03、F-2D-14 バッチ & バリアント** | **反映済（本更新で拡張）** |
| **`../2D_2_VARIANT_BATCH_PLAN.md`** | **fingerprint は識別子・表示名・更新時刻に依存せず構造と対象データの決定的値から作り、自動競合解消に使わず更新可否と警告の根拠にだけ使う／再生成で ID を再採番せず、追加は一度だけ生成し削除は事前提示／Source の追加・削除・上書きを要する差分は理由付き ineligible／write-set 内に手動変更が 1 件でもあれば write-set 全体を保護し項目単位の自動マージを行わない／実行前に全対象を分類（適用可・警告・対象外・手動調整あり・更新不要）し理由と変更内容を正本変更前に提示／手動調整ありは既定除外・警告は明示確認必須／確定は全件原子的で開始後の部分成功を許可しない／初期選択を全体へ広げず上限を設ける／準備中は取消可・確定開始後は取消不可を明示／1 一括 = 1 履歴で Undo も同じ原子経路／変更前に復旧点を作り作れなければ確定しない** | **F-2D-14 バッチ & バリアント** | **反映済（本更新）** |
| **`../2D_2_RASTER_REPAIR_REPORT.md` §8** | **palette 抽出は入力非破壊・決定的（量子化・代表平均色・画素数・coverage）／alpha しきい値で透明の扱いを明示／色数上限を設ける／色置換の対象を明示し暗黙に全 layer へ広げない／一時 palette state は保存データに混ぜない／重い解析は Worker へ逃がし同期 fallback を持つ** | **F-2D-13 パレット / ディザ** | **反映済（本更新）** |
| **`adr/0004` trim/atlas/scale 出力意味論** | **出力は派生であり元を壊さない／atlas 化・trim・出力倍率は「出力層の操作」で共通アセット座標・キャンバスサイズ・原点/アンカー/当たり判定の保存値を変えない／origin・anchors・colliders は変換や trim オフセット補正なしでパススルー／既定は cell 単位（trim なし・回転なし）／trim を有効にする出力は 4 記録（元フレームサイズ・切り出し矩形・atlas 内の位置とサイズ・復元用変換情報）を必須／回転は既定で使わない／同じ意味の設定を二重に持たず既存フィールドへ配線する** | **F-CORE-03、F-2D-06 ベクタ / 図形、F-2D-12 Atlas パッキング、F-CORE-07 書き出し** | **反映済（本更新で拡張）** |
| `adr/0006` 移行・復旧境界 | 移行前の退避と復旧の境界 | F-CORE-05 | 反映済 |
| `adr/0015` 移行詳細契約 | 段階的・決定的な migration | F-CORE-05 | 反映済 |
| `adr/0012` 拡張と未知データ | 未知データの非破壊往復 | F-CORE-05（`D3`/`D4`） | 反映済 |
| **`adr/0019` optional source mime と asset 0.2.0** | **取り込み元を verbatim 保持するために version を上げた前例**（原本を捨てず、schema を後追いで正す判断） | **F-CORE-05（版上げ）/ F-CORE-03（Source 保持）** | **反映済（本更新）** |
| **`adr/0014` validation staging** | **三段検証（構造 / 意味 / 出力 preflight）と、段ごとに異なる失敗時の既定動作。検証は read-only。秘密情報検出は preflight 段。不正入力対策は別関心事** | **F-CORE-06 検品フレーム** | **反映済（本更新）** |
| `adr/0013` / `adr/0017` provenance・AI 境界 | 記録境界と AI の線引き | F-CORE-05, 非目標の AI 境界 | 未（参照予定） |
| **`adr/0016` / `adr/0020` import 任意形式の分類・製品挙動** | **分類は「どう扱われるか」で表示し未対応は理由+代替手順／未実装を「対応済み」と表現しない／reference-only は誤解を生むため採らない／能動的構造は取り込み前に理由付き拒否／原本は書き換えずsanitizeせず verbatim 保持／codec 起動前の bounded preflight と上限超過の理由付き拒否／全件成功でのみ確定し 1 件失敗なら何も保存しない／部分 decode 失敗を成功扱いしない／時間は決定的に写像し loss を確定前に開示し確認を求める** | **F-2D-02 画像取り込み** | **反映済（本更新）** |
| **`adr/0018` chameleon atlas semantic reimport** | **自形式シートは読み直せるが「配布物を元のプロジェクトへ戻す操作ではない」（新 ID 体系の平坦化アセットを作る）／復元できないもの（構造・変形・タグ・リグ・出所・識別子・可変時間）を列挙し確認を必須／原本 verbatim・非保持データは常時 loss 表示／受け入れ条件を厳格に定め外部形式を「対応」と表現しない／現行の書き出し結果すべてが読み直せるとは表現しない／失敗時は何も変更しない／隔離は画像破損に限り構成不一致は理由付き拒否／tileSize 非除算は warning・当たり判定を自動生成しない** | **F-2D-11 tileset / sprite sheet 整理、F-2D-12 Atlas パッキング** | **反映済（本更新）** |
| **`adr/0021` frame duration semantics** | **可変時間・実効時間の規範／同一 Frame の全出現に同じ時間（出現別が必要なら複製）／合計時間は informational で正本にしない／複製は並びと event を変えず遡及復元しない／event は各出現・各周回で発火／固定 fps へ黙って均一化せず loss は理由付きで拒否（確認して均一化は不採用）** | **F-2D-07 フレームアニメーション** | **反映済（本更新）** |
| **`adr/0022` rig flip と bake parity** | **反転式（軸=原点x・pivot 鏡映・姿勢反転・可動域入替・時間不変）／完全 map による ID 再採番と全参照張り替え／未解決参照・循環・非有限値を黙って落とさず理由付きで拒否／parity は絶対差のみで相対許容を使わず配列順・参照・時間は完全一致／正規化してよい対象を限定（比較を緩めない）／bake 前の構造 preflight と生成量の事前見積り／拒否時は何も変更しない** | **F-2D-08 簡易リグ焼き込み** | **反映済（本更新）** |
| **`adr/0023` part / layer 差し替え** | **Part identity を維持し Layer/Texture/Frame を作成・削除・再採番しない／別 Part が所有する Layer は理由付きで拒否し暗黙に奪わない／bake 済みは変えず次回 bake から反映／write-set を対象 Part に限定／1 確定 = 1 履歴操作** | **F-2D-04 レイヤー & パーツ** | **反映済（本更新）** |
| `../2D_3_TIMELINE_RIG_PLAN.md` | timeline / rig の契約計画、slice と Gate の切り方 | F-2D-07 / F-2D-08 | 未（参照予定） |
| **`../2D_3_H3_MEASUREMENT_PROTOCOL.md`** | **固定 fixture・固定 matrix・Node / ブラウザ測定・結果 schema・停止条件（`tools/h3/`）** | **F-PLT-04 計測 & 退行検知（`PF1`/`PF2`）** | 未（参照予定） |
| **`../2D_EXPORT_COMPATIBILITY_MATRIX.md`** | **「対応」という言葉を曖昧にしない／互換性ラベル（`generic`/`candidate`/`verified`）と `verified` の必要条件（対象名・バージョン・検証日・環境・素材種別・fixture・取り込み手順・期待結果・自動/人手確認の範囲・既知の制限・再検証条件）／記録が無いものを verified と呼ばない／verified は対象バージョンに紐づき永続的な宣伝文句ではない／対象ツール名でない汎用出力はプロファイル名と確認環境を記録するまで generic のまま／自形式 atlas を外部標準形式と名乗らない／未対応は理由と代替手順つきで明示的に断る／対象プロジェクトの内部ファイルを無検証で生成しない** | **F-CORE-07 書き出しフレーム、F-2D-16 書き出し preset、F-2D-15 2D 検品** | **反映済（本更新で拡張）** |
| **`../EXPORT_QUALITY_DESIGN.md` / `../../EXPORT_FORMATS.md` / `../../ENGINE_INTEGRATION.md`** | **出力品質の options 設計、対応形式、エンジン連携の考え方／既定値での出力を現行と同一に保ち新オプションは省略可能な追加とする／保存データは常に等倍の正本／atlas format・preset schema の版上げや ZIP 構成変更は人間確認へ戻す** | **F-CORE-07, F-2D-16 書き出し preset, F-3D-15** | **一部（F-CORE-07 / F-2D-16 で参照。3D 側は未）** |
| **`../2D_COMPLETE_PRODUCT_SPEC.md` §5.5 / §5.6 / §6** | **ゲームエンジンを作らないが、書き出す前に素材がゲームでどう見えるかを確認できる必要がある／原点・アンカー・判定を重ねて再生、タイルの継ぎ目・ループを確認／画像サイズ・透明縁・命名・フレーム・判定・アンカー・tileSize・atlas 余白を理由つきで検査／修正案は自動確定せずプレビューと取り消しを可能にする／アセット契約（対象と素材種別ごとの必要条件）が揃うまで完成扱いにしない／出力先ごとに必要ファイル・座標変換・制限・対象バージョンを明示／同じ編集元と同じ設定から意味の同じ出力を再生成できる／plugin より先に「ファイル一式 + 検証済みの手動取り込み手順」** | **F-2D-15 2D 検品、F-2D-16 書き出し preset** | **反映済（本更新）** |
| **`../3d/3D_DECISION_LOG_AND_OPEN_ITEMS.md` 7 章（整合リスク台帳 `3D-RISK-01`〜`12`）** | **保存は常にモデル自身の空間で行い、見えている空間の値で保存しない／画面操作から保存値への変換は 1 か所に集約し、表示変換を変えても保存値が不変であることを往復で確かめる（`3D-RISK-09`）／変換は派生生成時に一度だけ適用し記録する（表示で 1 回・書き出しで 1 回の二重適用を防ぐ。`3D-RISK-01`）／内部要素への結び付けは宣言点を 1 か所に限り、照合できなければ「未解決」と表示して黙って別要素へ結び付けない（`3D-RISK-02`）／派生は連なるため直接の親を保持し、親の変化で古さを判定し、子を持つ派生の削除をそのまま通さない（`3D-RISK-07`）／しきい値・上限値を複数箇所に持たず確定値の所在を一つに定める（`3D-RISK-03`）／秒単位の処理に例外を作らない（`3D-RISK-08`）／基準（足元・前方向・単位）未確定のまま依存工程を始めさせない（`3D-RISK-12`）／利用条件は利用者申告が正で、埋め込み情報との矛盾は警告のみ・自動上書きしない（`3D-RISK-05`）／2D と 3D で語彙が同じでも実装・型・テストを共有しない（`3D-RISK-06`）／エンジン別の骨・影響数・命名の制約は同じ定数を検品・書き出し前検査・持ち込み手順書の 3 か所で参照する（`3D-RISK-11`）** | **F-3D-03、F-3D-05、F-3D-06、F-3D-07、F-3D-10、F-3D-11（今回反映）／F-3D-08/09/15（未詳細化）** | **反映済（本更新）** |
| **`../3d/README.md`（12 文書の 3D 計画一式）** | **3D は「作る」道具ではなく「ゲームで使える素材へ整える」道具／2D Pro Gate 承認まで実装・ライブラリ評価・dependency 追加・試作を開始しない／四段階（Gate → 最低限一続き → 実用 → 高度検品と軽量化 → 完成宣言）／2026-07-20 第2改訂で「骨入れは外部ツールの仕事」を改め、骨格フィット・自動ウェイト・モーション付与／流用をツール内で完了する計画へ変更（**本コンセプトへ取り込むかは `APP-O-04` として人間判断待ち**）** | **F-3D 群全体の位置づけ、`APP-O-04`** | **一部（README と 7 章リスク台帳を反映。下記 9 文書は未）** |
| **`../3d/3D_IMPORT_INSPECTION_SETUP_EXPORT_SPEC.md` §4 / `3D_FOUR_STAGE_IMPLEMENTATION_PLAN.md`（`3D-STAGE2-02`/`-04`）** | **指摘 1 件は「対象（種別・位置・名前）／実測値と推奨値とどの前提に対する評価か／理由／その先で何が起きるか／どうすれば直るか・このアプリで直せるか／無視してよい条件／検査規則の版」を持つ（3D 由来だが 2D にも有効なため Core の作法へ引き上げ）／検査は元のバイト列に対して行い、派生には同じ検査を再実行して比較する／構造は階層表示・検索・キーボードで辿れ、選択が表示と連動し、読み上げに載る／表示 / 非表示は一時状態で保存データに書かない／階層編集・名称変更は非範囲／自動修正は非範囲** | **F-CORE-06（FR-7a〜7f）、F-3D-04、F-3D-03、F-3D-12** | **反映済（本更新）** |
| **↑ 同（`3D_IMPORT_INSPECTION_SETUP_EXPORT_SPEC.md` の §5・§8・§9）** | **検査記録は追記型で、どのバイト列に対する検査かを必ず紐づけ、最新 1 件は必須・履歴は容量を見て任意／既定ではモデルを取り込んだままの姿で出し、整備した基準は付随データとして同梱して変換は受け取り側の作業として手順書に書く／焼き込みは既定無効、骨・動き付きモデルでは特に警告／目録に含まれるファイル・大きさ・特定値・役割と、元モデルの特定値と原ファイル名を持つ／再現可能な出力（並び・時刻・整形の固定、毎回変わる値は目録の 1 か所にだけ置き他ファイルへ埋め込まない、再現できない操作は理由を明示、2 回書き出して一致を確かめる）／説明を書いただけの対象を「確認済み」と表示しない／一式の中の位置は相対のみで上位を辿る表記・制御文字を含めず、同名衝突は決定的な規則で解決／書き出し前に妥当性を確かめ不正なら一式を作らない、元モデルが見つからなければ中止して復旧手段を案内、進行表示と取消** | **F-3D-14 記録、F-3D-15 書き出し、F-3D-13 器** | **反映済（本更新）** |
| **↑ 同（`3D_IMPORT_INSPECTION_SETUP_EXPORT_SPEC.md` の §1〜§3）** | **中心形式へ集約する判断の理由（表現力の差を抱えたまま「対応」と言わない）／重い専用形式を「一応読める」で抱え込まない／束は複数ファイル選択と書庫の 2 通りで受け取り、フォルダ操作は補助で必須経路にしない／参照は同時に渡された集合の中でだけ解決し、外部の所在は解決せず警告のうえ欠落として続行／上位を辿る表記等を拒否／書庫は展開後サイズと項目数の上限を持つ／拡張子や宣言種別を主根拠にしない／拒否は資源枯渇に直結するものに限り、それ以外は警告のうえ表示を試みる／上限は暫定として扱い実測で確定する／未知の塊は保持したうえで警告／隔離は件数と容量の上限を持つ／中断は常に可能で途中生成物を残さない／書き出した一式は通常の取り込み経路の入力形式ではない（往復確認はテストで行う）** | **F-3D-02 多形式取り込み、F-3D-01 安全読み込み** | **反映済（本更新）** |
| **`../3d/3D_UI_UX_SPEC.md` §1・§6〜§8** | **Canvas だけに情報を依存させない——状態・選択・範囲・警告・付与情報は必ず一覧・数値・文章でも確認でき、視覚的に確認できない利用者もすべての作業を完了できる（後付けの補助ではなく設計の制約）／代替情報は常設とし、流れて消える通知だけに頼らない／描画面にも要約を持たせ詳細は隣の一覧で読める／すべての状態を色だけで区別せず記号と文言を併用／待たせる処理は進捗・中断・失敗理由を必ず持つ／破壊的に見える操作は前後の数値を示し取り消せる／動きを抑える設定を尊重し自動回転・自動再生・滑らかな移動を止める／タップ対象の大きさを確保／削除は確認し取り消せることを明示／ドラッグ中の意図しない接触はドラッグ優先／状態表示の共通形（空・処理中・成功・警告・失敗・保存状況）／編集操作はすべて共通の履歴経路に載せ、一時状態（視点・選択・タブ）は保存しない** | **F-UX-01 デザインシステム、F-UX-02 アクセシビリティ、F-UX-03 メッセージ** | **反映済（本更新）** |
| **`../3d/3D_INTEROP_VRM_VR_AND_CREATION_SPEC.md` §4〜§5** | **人型スロットは既存の標準語彙を採用し独自語彙を作らない（独自語彙は流用も往復も難しくする）／対応付けは番号を正・名前は表示と再確認用／名前からの自動推定は「候補の提示」までとし確定は必ず利用者操作／骨を足した派生は構成が変わるため対応付け・アンカー・判定を派生側の番号へ再バインドする必要がある（実装時の最頻出バグ想定箇所）／付与は基準（足元・前方向・単位）の確定を前提条件とする／頂点単位のウェイト編集・骨の自由な追加削除・揺れもの作成・本格リグ制作は外部ツールの仕事／人型アバター形式は拡張を未知データとして保持すれば素通しで成立する／内包された利用条件は参考として転記し、利用者申告を正として自動上書きせず、食い違いは警告のみ／内包内容の正しさを検証・保証しない（記録と表示まで）／未記入は警告する／書き出しは素通しのみで、利用条件を書き換えた派生を作らない／拡張を反映した表示は別判断で、通常形式として表示できれば完成条件は満たす** | **F-3D-08 骨格検査、F-3D-09 人型アバター形式、F-3D-06 アンカー、F-3D-13 metadata** | **反映済（本更新）** |
| `../3d/3D_COMPLETE_PRODUCT_SPEC.md` / `3D_ARCHITECTURE_AND_BOUNDARIES.md` / `3D_ASSET_DATA_CONTRACT.md` / `3D_IMPORT_INSPECTION_SETUP_EXPORT_SPEC.md`（§6〜§7） / `3D_UI_UX_SPEC.md` / `3D_PERFORMANCE_DEVICE_SECURITY_LICENSE_SPEC.md` / `3D_TEST_EVIDENCE_AND_RELEASE_SPEC.md` / `3D_INTEROP_VRM_VR_AND_CREATION_SPEC.md`（§4〜§5 以外） / `3D_FOUR_STAGE_IMPLEMENTATION_PLAN.md`（上記 WP 以外） | 完成条件・非目標／2D-3D 境界と module・lazy load・Worker／データ契約（座標・anchor/collider・migration・不変条件）／対応形式と受け取り方・安全な読み込み・Setup・Game Data／画面と操作／性能予算・端末・安全・ライセンス／テスト階層と証拠／VRM・VR・ボーン・テクスチャ編集・画像→3D／残りの work package | F-3D 群の残り（`02`/`08`/`09`）と既に詳細化した機能の再確認 | **未（詳細化のたびに該当文書を先に読む。`F-3D-04` でこの運用を開始し、`F-3D-13`〜`15` でも継続）** |
| **`../THREE_D_ASSET_PREPARATION_REQUIREMENTS.md`** | **（§4）`unit` / `upAxis` / `forwardAxis` は `unknown` を取り得る型として持ち、推測で埋めない／`originMode`（中心 / 足元 / 任意）・`scale`・`bounds`／（§4.1）アンカーは位置必須・向き任意で role を持ち、用途は武器・手・足元・弾発射位置・カメラ注視点など／（§4.2）判定は box / sphere / capsule に限定し、初期は mesh collider を作らない（重くなりやすくエンジン差も大きい）／（§6）初期は自動で強い破壊的最適化をしない・元ファイルを保持する・最適化前後の差を表示する・見た目が変わる処理は必ずプレビューしてから適用する・圧縮方式は候補提示に留める／（§11）optimized model を必須にしない／3D の最初の価値は生成ではなく検品と軽量化（生成 3D の出力先ではなく、生成 3D をゲーム素材へ整える場所）／生成 AI を初期依存に入れず外部処理として扱う／元ファイルを保持し最適化で破壊的に上書きしない／読み込み中に UI を固めず進捗と失敗理由を表示／3D 関連は 2D の負荷を増やさない（lazy load）／特定 GPU API を必須にしない／検品必須項目（サイズ・頂点数・三角形数・マテリアル数・テクスチャ枚数と最大サイズ・アニメーション数・bounds・原点）と理由つき警告／しきい値は固定しすぎず対象環境プリセットで持つ／初期は mesh collider を作らない／ライセンスはコード・モデル重み・学習データ制限・商用条件・再配布条件まで確認し、未確認の候補を dependencies に追加しない／2D の保存データを 3D のために壊さない** | **F-3D-01 安全読み込み、F-3D-03 ビューポート & 検品表示（以降の F-3D 群も参照）** | **反映済（本更新。F-3D 群の詳細化に伴い順次拡張）** |
| `../2D_DEVICE_RELIABILITY_SPEC.md` | 端末別の保存・復旧・性能・アクセシビリティの見方 | F-UX-04, F-CORE-02 | 未（参照予定） |
| `../PERFORMANCE_BUDGET.md` | 現状性能 baseline と再測定手順 | F-PLT-04 | 未（参照予定） |
| **`../EXTERNAL_GITHUB_REFERENCE_CATALOG.md`** / **`../EXTERNAL_GITHUB_REFERENCE_REVIEW_2026-07-26.md`** | **評価は「役立つ度合い」であって「採用してよい度合い」ではない／採用検討時は対象 commit を固定し、ライセンス・モデル重み・商用と再配布条件・動作条件に加え「元データを保持できるか・処理前後を比較できるか・失敗時に正本を壊さないか」まで再確認する（設計適合の条件）／冒頭のライセンス宣言だけで判断しない（MIT と書きつつ末尾に表示義務がある実例）／ゲーム制作にとっての有用性と本コンセプトにとっての有用性を分ける／外部エンジンは組み込む部品ではなく持ち込めるかを確認する対象／一覧は軽い縮小画像で選択時だけ実体を起動し、一覧で 3D 描画基盤や重い復号器を初期化しない／代表画像と実体を同一物として扱わない／署名の確認だけで内容や権利を合格にしない／分布（p95/p99 相当）で見て中央値だけで合格にしない・引っかかりを原因つきで記録・時刻/種/品質/視点/予算を固定し隔離して測る・端末種別ごとに別々に測る／説明できない出力差を伴う軽量化を採用しない・見た目の一致だけで意味上の正しさを合格にしない／導入の原子性は準備中の置き場・退避した旧版・未完了の印で担保し、識別子に経路表記を許さない** | **`APP_COMPETITIVE_LANDSCAPE.md` 6 章、F-CORE-08、F-PLT-04、F-3D-01、F-3D-12、F-MOD-02** | **反映済（本更新で突き合わせ完了）** |

### 6.1 特に継承する「進め方」

`2D_3_H3_MEASUREMENT_PROTOCOL.md` は **「測定方法は固定する。数値 budget は未決のまま残す」** という姿勢を採っている。これは本コンセプトの性能規律（`APP_MODULAR_ARCHITECTURE.md` 7 章：仮値を置き、実測で置換する）と同じ考え方であり、**F-PLT-04 の詳細化では、数値を先に決めずに測定手順・固定 fixture・結果 schema・停止条件から固める**方針を継承する。

---

## 7. 現在の状況

- 詳細化済み: **F-CORE-01 / 03 / 04 / 05**（Core 土台）、**F-MOD-01 / 02 / 03 / 04**（install-on-demand の中核）、**F-CORE-09 / 10**（Extension Host と権限の Core 受け皿）。
- **モジュール機構が一巡した**: 宣言（manifest）→ 発見 → 導入/撤去 → 依存と重さの誠実な開示 → 入れてよいものかの検証 → 登録とライフサイクル → 権限の強制。「多機能と軽量の両立」（`P2`→`P3`→`P4`）を支える骨格が、要件レベルで通った。
- 上流知見の取り込み: 6 章の系譜マップを新設し、ADR-0019（原本保持のための版上げ）を F-CORE-03 / F-CORE-05 に反映済み。ADR-0021 / 0022 / 0023 と `2D_3_H3_MEASUREMENT_PROTOCOL.md` は、対応する機能（F-2D-07 / 08 / 04、F-PLT-04）の詳細化時に参照する。
- **F-PLT-04 完了**（`features/F-PLT-04-performance-measurement-regression.md`）。ここまでの機構が掲げる「未使用ゼロコスト」「撤去でコストが戻る」「宣言 footprint との突き合わせ」を、**主張から証拠へ変える物差し**が定まった。6 章の系譜マップに従い `2D_3_H3_MEASUREMENT_PROTOCOL.md` の進め方（**測定手順と停止条件を先に固定し、数値は実測 + 人間承認で置換**）を継承している。
  - **順序の修正**: 当初は「F-PLT-01 外殻 → F-PLT-04 計測」の順を想定していたが、`APP-R-01` が外殻方式を**実測後に確定**と定めている以上、**候補を測る物差しが先**でなければ外殻を評価できない。よって F-PLT-04 を先に確定した。
- **F-PLT-01 完了**（`features/F-PLT-01-native-shell-os-integration.md`）。外殻の**責務境界**（担うこと / 担わないこと）と**選定基準**（性能・描画適性・OS 差・ライセンス・体制・セキュリティを全項目確認し、単一指標で決めない）を固定した。**採用そのものは未決**——実測 + 人間承認 + ADR 記録が要る（`APP-R-01` / `APP-O-03`）。
- **F-PLT-03 完了**（`features/F-PLT-03-heavy-work-offload.md`）。**8 機能が「UI を固めない」を掲げながら、その実現手段の正本が無い**状態だったため優先した（最も多く依存されていた未定義機能）。キャンセル可能・中断しても壊さない・重い処理を 1 履歴単位として扱う・高速経路は任意で fallback で成立・モジュールの重い処理も同じ仕組みに乗せる、を要件化。
- **F-PLT-02 完了**（`features/F-PLT-02-native-file-io-dialogs.md`）。F-CORE-01 FR-8 / F-CORE-05 FR-12 / F-MOD-02 FR-7 が前提にしていた**原子的に書ける手段**の正本。中断しても直前の保存を壊さない、部分書き込みを検出する、元ファイルを読み取りのみで扱う、D&D・ネイティブダイアログ・関連付け、ZIP 書き出しも途中失敗で不完全な成果物を残さない、を要件化。
- **F-PLT-05 完了**（`features/F-PLT-05-edition-renderer-loading.md`）。**F-PLT 群（プラットフォーム基盤）が揃った**。起動時はどちらのレンダラも載せない・使わないエディションの描画依存を常駐させない・レンダラ依存を Core/Shell へ染み出させない・切り替えで資源を解放する・描画不能でもデータを失わない、を要件化（採用は `APP-O-03` のとおり未決）。
- **F-CORE-07 完了**（`features/F-CORE-07-export-frame.md`）。制作物が外へ出る唯一の出口であり、差別化仮説 2（「作れる」ではなく**外部エンジンで確実に使える**）を支える器。上流の**互換性ラベル（`generic`/`candidate`/`verified`）と「記録が無いものを verified と呼ばない」規律**を継承した。
- **F-CORE-06 完了**（`features/F-CORE-06-inspection-validation-frame.md`）。ADR-0014 の**三段検証（構造 / 意味 / 出力 preflight）と段ごとに異なる失敗時の既定動作**（構造=止める／意味=修復要求し黙って直さない／出力=警告 or 停止だが保存正本は止めない）、**検証は read-only**、**秘密情報検出は preflight 段**、**不正入力対策は別関心事**、を継承した。
- **これで `A2`（Core 契約）の全要素が揃った**（保存・非破壊・履歴・形式/移行・検品・書き出し・Host・権限）。
- **F-CORE-08 完了**（`features/F-CORE-08-home-project-dashboard.md`）。利用者が最初に触れる画面であり、`P1`（非採用の法則）の複数項目——起動の速さ・迷わなさ・見て分かるか——が**同時に評価される場所**。`A3` の**エディション分岐点**でもあり、**Dashboard 表示時点ではどちらのレンダラも載せない**ことを要件化した。
- **F-CORE-02 完了**（`features/F-CORE-02-autosave-crash-recovery.md`）。ADR-0006 の**「ローカル作業コピー」と「可搬正本」の区別**を継承し、**自動保存は利用者の保存ファイルを上書きせず別の復旧領域へ書く**ことを核に据えた。復旧するかは利用者が選ぶ（黙って上書き復元しない）、破壊的操作前の復旧点、保持と掃除の方針、自動保存で「保存済み」と誤認させない、も要件化。
- **🎉 Core 群（F-CORE-01〜10）が完了**。土台（保存・非破壊・履歴・形式/移行・検品・書き出し・Dashboard・Host・権限・自動保存）がすべて「詳」になった。
- **2D パイプライン着手**: **F-2D-01 完了**（`features/F-2D-01-project-creation.md`）。上流方針が繰り返し強調した「**Chameleon は画像取り込み専用の変換ツールではない**」が最初に形になる場所。5 つの起点（空 / テンプレート / 画像 / 図形 / 既存アセット）を揃え、**起点は入口であって牢屋ではない**（後から他の要素を足せる）ことを要件化した。
- **F-2D-03 完了**（`features/F-2D-03-canvas-viewport.md`）。全 2D 編集が載る面であり、**座標の正本を守る**責務を負う——原点・アンカー・当たり判定は「画面のどこを指したか」が意味を持つため、**画面座標とデータ座標の対応が信頼できなければ、それらの機能はすべて信用できなくなる**。ADR-0001 から**共通アセット座標**、**grid/snap は UI 補助で保存座標の意味を変えない（開いただけで丸めない）**、**出力先の Y 軸・pivot 差はキャンバスで吸収せず書き出し側が担う**を継承した。
- **F-2D-04 完了**（`features/F-2D-04-layers-parts.md`）。F-2D-03 が「どこ」を担うなら、本機能は **「何に対する操作か」** を担う。ADR-0023 から **Part identity の維持**、**Layer/Texture/Frame を作成・削除・再採番しない**、**別 Part が所有する Layer は理由付きで拒否し暗黙に奪わない**、**bake 済みは変えず次回 bake から反映**、**write-set を対象 Part に限定**、**1 確定 = 1 履歴操作** を継承した。
- **F-2D-09 / F-2D-10 完了**（`D-DONE-3` の対）。**絵をゲーム素材に変える情報**の付与。ADR-0010 から **上書きはフレーム単位のみ**（アニメーション単位を導入しない理由 = 同じフレームを複数アニメが共有すると**フレームの意味が分裂する**）、**上書き範囲は位置・サイズ・表示可否に限定**、**polygon は導入前チェックリストを決め切るまで有効にしない**（ただし rect/circle だけで制作が完了する）を継承した。
- **F-2D-07 完了**（`features/F-2D-07-frames-animation.md`）。ADR-0021 から**可変時間と実効時間の単一規範**、**同一 Frame の全出現に同じ時間**（出現別が必要なら複製 = `F-2D-10` と同じ「意味を分裂させない」思想）、**合計時間は informational で正本にしない**、**複製は並びと event を変えず遡及復元しない**、**event は各出現・各周回で発火**、そして最重要の **固定 fps へ黙って均一化せず、loss は理由付きで拒否**（「確認して均一化」は上流でも不採用）を継承した。
- **F-2D-08 完了**（`features/F-2D-08-rig-bake.md`）。**`D-DONE-4` が揃った**。ADR-0022 から**反転式**（軸=原点x・pivot 鏡映・姿勢反転・可動域入替・時間不変）、**完全 map による ID 再採番と全参照張り替え**、**未解決参照や循環を黙って落とさず理由付きで拒否**、**parity は絶対差のみで相対許容を使わず、配列順・参照・時間は完全一致**（**比較を都合よく緩めない**）、**bake 前の構造 preflight と生成量の事前見積り**、**拒否時は何も変更しない**を継承した。
- **F-2D-02 完了**（`features/F-2D-02-image-import.md`）。**外部の信頼できないファイルがアプリに入る唯一の入口**であり、安全性・正直さ・非破壊が最も試される場所。ADR-0016/0020 から**分類は「どう扱われるか」で表示**・**reference-only を採らない**（容量を消費しながら編集できず対応していると誤解させるため）・**能動的構造は取り込み前に理由付き拒否**・**原本は sanitize せず verbatim 保持**・**bounded preflight**・**全件成功でのみ確定**・**部分 decode 失敗を成功扱いしない**・**loss を確定前に開示し確認を求める**を継承した。
  - **本コンセプト固有の差**: 上流は保存形式の制約から時間を一律 fps へ写像していたが、本コンセプトは可変時間を持てる（`F-2D-07`）。継承したのは「一律化する」制約ではなく **「決定的に写し、失うものを必ず示す」規律**であり、**可能な範囲で元の時間を保持**する。
- **F-2D-05 / F-2D-06 完了**。`D-DONE-2` の編集実体と `D-DONE-1` の図形起点。ラスタは **1px 精度・意図しない補間を既定にしない・1 ストローク = 1 履歴**、ベクタは **解像度非依存の数値定義・ラスタ化は派生で定義を失わない・出力倍率や trim は保存値を変えない**（ADR-0004 の境界）を要件化。**取り込んだ SVG が画素になること**と**アプリ内図形が編集できること**の違いを明示する点も入れた（ADR-0016 の `editable-import` 別判断を継承）。
- **✅ 上流の `EXTERNAL_GITHUB_REFERENCE_CATALOG.md` / `EXTERNAL_GITHUB_REFERENCE_REVIEW_2026-07-26.md` との突き合わせ完了**（`APP_COMPETITIVE_LANDSCAPE.md` 6 章に結果を記録）。**両者は競合せず役割が違う**——本コンセプト側は「どこで差を出すか」、上流は「設計時に何を読むか」の索引。継承した規律と、そこから既存 5 機能へ反映した具体（`F-CORE-08` の一覧の軽さ、`F-PLT-04` の分布と隔離測定、`F-3D-01` の署名だけで合格にしない、`F-3D-12` の説明できない差を採らない、`F-MOD-02` の導入の原子性と識別子の制限）は 6 章の系譜マップに登録した。
  - **強い一致**: 上流の `IDEA-3D-01`「`Import → Inspect → Optimize → Compare → Export` の非破壊 pipeline」は、本コンセプトが独立に組み立てた `F-3D-01` → `F-3D-03` → `F-3D-10`/`11` → `F-3D-12` → `F-3D-15` と一致した。**別々の経路で同じ形に到達した**ことは構造の妥当性の傍証になる。
  - **意図的に取り込まないもの**: 外部生成 AI 連携（`IDEA-3D-02`/`05`）、world / voxel mode（`IDEA-3D-06`/`07`）、音声起点の生成。`G2` の AI 境界と Vision 7 章の非目標に当たる。**取り込まない判断も記録として残す**。
- **F-2D-11 / F-2D-12 完了**（`D-DONE-5` の対）。**ばらす（切り分け）** と **まとめる（詰め直し）** が揃った。
  - ADR-0018 から: **自形式シートは読み直せるが「配布物を元のプロジェクトへ戻す操作ではない」**（新しい ID 体系の、平坦化されたアセットを作る操作）、**復元できないものを列挙して確認を必須にする**、**原本 verbatim・非保持データは常時 loss 表示**、**受け入れ条件を厳格に定め、外部形式を「対応」と表現しない**、**現行の書き出し結果すべてが読み直せるとは表現しない**、**失敗時は何も変更しない**、**隔離は画像破損に限り構成不一致は理由付き拒否**、**tileSize 非除算は warning・当たり判定を自動生成しない**を継承した。
  - ADR-0004 から: **atlas 化・trim・出力倍率は「出力層の操作」であり保存座標を変えない**、**原点 / アンカー / 当たり判定は変換や trim オフセット補正なしでパススルー**、**既定は cell 単位（trim なし・回転なし）**、そして最も効くゲートとして **trim を有効にする出力は 4 記録（元フレームサイズ・切り出し矩形・atlas 内の位置とサイズ・復元用変換情報）を必須**——「ファイルが小さくなる」ことは判定がずれる理由にならない——を継承した。**同じ意味の設定を二重に持たない**（上流 `ExportPreset.scale` の前例）も要件化した。
- **F-2D-13 / F-2D-14 完了**。**「色を制約する」**と**「量に適用する」**。
  - `F-2D-13` は **モジュールとして足す機能の最初の実例**であり、`P4`（未使用ゼロコスト）と `D3`（撤去してもデータが往復する）を**機能単位の受け入れ条件へ落とした最初の機能**でもある（AC-9〜AC-12）。上流 §8 から**入力非破壊で決定的な palette 抽出**、**alpha しきい値で透明の扱いを明示**、**色数上限**、**置換対象を明示し暗黙に全体へ広げない**、**一時 palette state を保存データに混ぜない**を継承した。ディザには上流前例が無いため、**決定性という要件だけを固定**し方式は実装時判断とした。**編集時のパレット制約**と**書き出し時の減色**を混同しない境界（`F-2D-12` FR-1 と同じ）も入れた。
  - `F-2D-14` は**最も壊しやすい機能**であり、重心を「便利さ」ではなく **「壊さないための条件」** に置いた。ADR-0003 から **関係の正本は 1 か所（optional / additive）**、**複製・反転コピーを自動登録しない**、**base 保存だけでは variant を更新しない**、**出力倍率違いを variant として複製しない**、**関係 metadata を出力へ入れない**を継承。`2D_2_VARIANT_BATCH_PLAN.md` から **write-set 内に手動変更が 1 件でもあれば write-set 全体を保護**（項目単位の自動マージを機械に決めさせない）、**fingerprint は自動競合解消に使わず警告の根拠にだけ使う**、**再生成で ID を再採番しない**、**実行前の対象分類と理由提示**、**手動調整ありは既定除外**、**確定は全件原子的で開始後の部分成功を許可しない**、**初期選択を全体へ広げない**、**1 一括 = 1 履歴**、**復旧点を作れなければ確定しない**を継承した。
- **🎉 2D エディション（F-2D-01〜16）が完了**。`D-DONE-1`〜`D-DONE-7` のすべてに担当機能が付いた。
  - `F-2D-15`（2D 検品）は **「出す前に、エンジンで起きることを先に見せる」** 機能。制作の失敗はエンジンへ入れた瞬間に判ることが多く、**気づくのが遅いほど手戻りが大きい**。上流 §5.5 から **「ゲームエンジンを作らない。ただし書き出す前に、素材がゲームでどう見えるかを確認できる必要がある」** という立場をそのまま継承し、**検査は read-only**（ADR-0014）、**理由つきの指摘**、そして最も効く **修正案を自動確定せず、プレビューと取り消しを可能にする**（「直しておきました」をしない）を要件化した。§6 の**アセット契約**——対象と素材種別ごとの必要条件が揃うまで完成扱いにしない——も入れ、`F-2D-16` の `verified` の根拠として接続した。
  - `F-2D-16`（書き出し preset）は **2D パイプラインの出口**。中心は形式の数ではなく、互換性マトリクス 1 章がまず置いた **「『対応』という言葉を曖昧にしない」**。**記録が無いものを `verified` と呼ばない**、**`verified` は対象バージョンに紐づき永続的な宣伝文句ではない**、**対象ツール名でない汎用出力は対象名で語らない**、**自形式を外部標準形式と名乗らない**、**未対応は理由と代替手順つきで断る**、**対象プロジェクトの内部ファイルを無検証で生成しない**を継承。ADR-0001 から **出力先の Y 軸・pivot 差はキャンバスで吸収せず書き出し側が担う**、EXPORT_QUALITY_DESIGN から **既定値での出力を変えず新オプションは省略可能な追加とする**（preset が増えても既存出力の意味が変わらない）と **保存データは常に等倍の正本**を継承した。**plugin より先にファイル一式 + 検証済みの手動手順**という順序も要件に入れた。
- **3D パイプライン着手**: **F-3D-01 / F-3D-03 完了**。3D は **「生成」ではなく「検品と軽量化」から入る**——上流 §15 の結論（**AI 生成 3D モデルの出力先ではなく、AI 生成 3D モデルをゲーム素材として整える場所**）をそのまま継承した。
  - `F-3D-01`（安全読み込み）は **3D パイプラインの入口**であり、`F-2D-02` と同じく**外部の信頼できないファイルが入る場所**。ただし 3D は 2D より危険で、**glTF は 1 ファイルで完結せず `.bin` とテクスチャを外部参照する**——**参照の解決範囲を限定しなければ、取り込みがファイル読み取り経路になる**。そこで**取り込み対象の外・上位ディレクトリ・外部ネットワークを指す参照の拒否**を FR に立てた。上流からは **元ファイルを絶対に上書きしない**、**bounded preflight と理由付き拒否**、**全件成功でのみ確定**、**読み込み失敗の理由を表示**、**特定 GPU API を必須にしない**、**ライセンスはコード・モデル重み・学習データ制限・商用条件・再配布条件まで確認**を継承した。
  - `F-3D-03`（ビューポート & 検品表示）は **全 3D 作業が載る面**であり、`F-CORE-06` の 3D 具体化。生成された 3D モデルは**そのままではゲームに入らない**（重すぎる・原点が足元にない・テクスチャが大きすぎる）——**その事実を最初に見せる場所**である。上流 §5 の**必須検品項目**と§5.1 の**理由つき警告**、そして **しきい値を固定しすぎず対象環境プリセット（モバイル / Web / デスクトップ）で持つ**を継承した。
  - **本コンセプト固有の差**: 上流は「3D 関連は 2D の bundle size を不必要に増やさない／可能なら lazy load」を**工夫**として書いていたが、本コンセプトは **エディション + install-on-demand の構造で満たす**（`P4` / `F-PLT-05`）。継承したのは「軽くする努力」ではなく **「3D を使わない利用者のコストを 1 バイトも増やさない」という不変条件**である。
- **F-3D-05 / F-3D-06 / F-3D-07 完了**（`T-DONE-4` が揃った）。2D の `F-2D-09`/`F-2D-10` に対応する 3D 版。
  - `F-3D-05`（基準整備）の核心は「正しい値を決めること」ではなく、**「判らないものを判らないまま扱えること」**。2D の座標は左上原点で一意に決められるが（ADR-0001）、**3D モデルは作られた環境ごとに up 軸も前方向も単位も違い、しかもファイルにその情報が無いことがある**。上流 §4 が `unit` / `upAxis` / `forwardAxis` すべてに `'unknown'` を持たせた前例を継承し、**推測で埋めない**・**不明のまま先へ進める**・**影響する場面では警告する**・**「ファイルがそう言っている」と「制作者がそう決めた」を区別する**を要件化した。整備は**解釈として保持**し元モデルのジオメトリを書き換えない。**対象ごとの軸・単位差はここで吸収せず書き出し側が担う**（ADR-0001 を 3D へ展開）。
  - `F-3D-06`（アンカー）は**モデルの形からは判らない情報**——武器・手・足元・弾発射位置・カメラ注視点——を扱う。上流 §4.1 の role を継承しつつ **一覧を閉じない**、**位置は必須・向きは任意**、**名前は一意**（ADR-0002）、そして **自動生成しない**（候補提示は可、確定は制作者）を要件化した。**骨格が無いモデルでも置ける**（骨との対応付けは `F-3D-08`）。
  - `F-3D-07`（当たり判定）で最も重要なのは**何を作らないか**。上流 §4.2 は形状を **box / sphere / capsule に限定**し **mesh collider を作らない**と決めた——**重くなりやすく、エンジン差も大きい**から。これは 2D で **polygon を境界として扱った ADR-0010 と同じ判断構造**であり、**表現力の不足より「動くはずが動かない」を避ける**。形状追加は**導入前の確認条件（再現性・負荷・書き出し表現）を決め切ってから**とし、**限定形状だけで制作が完了する**ことを前提に置いた。
- **F-3D-10 / F-3D-11 / F-3D-12 完了**（`T-DONE-6` が揃った）。**3D の価値の両輪（検品と軽量化）が機能単位で揃った**。
  - `T-DONE-6` の文言そのものに設計が書かれている——**「元を壊さず」「別モデルを作り」「前後を比較して選べる」**。つまり**軽量化は置き換えではなく、選択肢を増やす操作**である。この読み方を 3 機能の軸に据えた。
  - `F-3D-10`（mesh 簡略化）: 上流 §6 から **初期・既定で自動的に強い破壊的最適化をしない**、**元ファイルを保持する**、**見た目が変わる処理は必ずプレビューしてから適用する**を継承。さらに **安全に外せるもの（未使用データ・不要ノード）と見た目に影響するもの（面の削減・統合）を区別**（性質が違うものを同じボタンにまとめない）、**失われ得るものを示す**、**決定的**、**失敗・取消で何も変えない**を要件化した。§11 の **optimized model を必須にしない**から **採用しない自由**も入れた。
  - **本コンセプト固有の追加**: 上流には無かった接続として、**簡略化しても `F-3D-05` の基準と `F-3D-06`/`F-3D-07` の付与情報が壊れない**を要件にした。これは **`F-3D-07` が判定を box / sphere / capsule に限定した利点**でもある——**判定がモデルの形に依存していないため、面を減らしても判定は影響を受けない**。限定という制約が、別の場面で保全という利益を生んでいる。
  - `F-3D-11`（テクスチャ圧縮）: 3D の重さは**面の数だけで決まらず、多くの場合テクスチャがメモリと読み込み時間の大半を占める**。上流 §6 の **候補提示（自動適用ではない）** と §5.1 の **4096px 超の警告**を継承しつつ、**用途ごとに許される劣化が違う**（見た目の色 vs データ）と**色空間・透明度の扱いを明示**を加えた。さらに `2D_EXPORT_COMPATIBILITY_MATRIX.md` の **「対応」を曖昧にしない**を 3D へ展開し、**記録の無い圧縮形式を「対応」と表示しない**・**対象環境で読めない可能性がある形式で警告する**（「軽いが動かない」を作らせない）・**対応状況は対象バージョンに紐づく**を要件化した。
  - `F-3D-12`（前後比較）: **この機能があることで軽量化の設計全体が成立する**。「勝手に強く削らない」と「プレビューしてから適用」は、**比較して選べる場所があって初めて実用になる**——比較が無ければ制作者は判断できず、結局軽量化を使わない。**並べて / 切り替えて / 重ねての見せ方を一つに固定しない**（破綻の見え方は場合による）、**同じ視点・同じ条件で比較**、**どこが変わったかへ導く**（全体を眺めて気づけとしない）、**対象環境プリセットに対する評価の変化**（「モバイルでは重すぎる」が「収まった」に変わったか）、**比較は read-only**、**自動で最良を選ばない**、**採用しない自由**、**採用は取り消せて元と他候補が残る**を要件化した。
- **⚠️ 見落としの是正（本更新）**: `docs/future/3d/` に **12 文書からなる完全な 3D 計画一式**が 2026-07-20 から存在していたが、本コンセプトの系譜マップには `THREE_D_ASSET_PREPARATION_REQUIREMENTS.md` しか登録しておらず、**`F-3D-01`〜`12` の 8 機能をこの一式を読まずに書いていた**。今回、`3d/README.md` と **7 章の整合リスク台帳（`3D-RISK-01`〜`12`）**を突き合わせ、既存 6 機能へ反映した。**残り 9 文書は未突き合わせ**として系譜マップに明記し、以降の詳細化では該当文書を先に読む。
  - **最も効いた継承**は `3D-RISK-09`（保存空間と表示空間の取り違え）。**向きを補正して表示している最中に置いた点を、見えたままの値で保存すると、補正を変えた瞬間に全部ずれる**。`F-3D-05` に「保存は常にモデル自身の空間」「変換は 1 か所に集約」「表示変換を変えても保存値が不変であることを往復で確かめる」を追加し、`F-3D-06`/`F-3D-07` にも同じ境界を引いた。
  - **既存記述の補正**は `3D-RISK-02`（結び付けの基準ずれ）。`F-3D-10` は「判定は形に依存しないので簡略化後も保全される」と書いていたが、**アンカーがモデル内部の要素へ結び付いている場合は簡略化で無効になり得る**。「未解決として示し、黙って別要素へ張り替えない」を FR-14a として補った。
  - **設計の穴の発見**は `3D-RISK-07`（派生連鎖）。`F-3D-10`/`11` は「複数候補を持てる」と書いたが、**派生が連なったときの親子関係・古さ判定・孤児化**を扱っていなかった。FR-14b〜14d を追加した。
  - **範囲の未決を新規登録**: `APP-O-04`（骨組みの当てはめ・肌の自動追従・動きの付与／流用を 3D の範囲に含めるか）。上流は第2改訂でツール内完了へ方針変更したが、本コンセプトの `T-DONE-5` は検査と対応付けまで。**人間判断が要る**ため、`F-3D-16`/`17`/`18` を「保留」としてインベントリに置いた。
- **F-3D-04 完了**。**是正後の運用（該当する上流文書を先に読む）を最初に適用した機能**であり、`3d/3D_IMPORT_INSPECTION_SETUP_EXPORT_SPEC.md` §4 と `3D_FOUR_STAGE_IMPLEMENTATION_PLAN.md` の該当 work package を先に読んでから書いた。
  - 役割は **`F-3D-03` との差**にある。概観は「重い」と言うが、**どこが重いのかが判らなければ直せない**。本機能は中身を開き、**問題箇所へジャンプできる**ようにする。`F-3D-10` の「安全に外せるもの」の判定根拠（未使用・重複）もここが供給する。
  - 上流から **表示 / 非表示は一時状態で保存データに書かない**、**階層編集・名称変更は非範囲**（整えるのであって作り変えるのではない）、**自動修正は非範囲**、**検査は元のバイト列に対して行い、派生には同じ検査を再実行して比較する**（別の物差しで比べない）を継承した。
  - **最大の収穫は「指摘 1 件が持つべき中身」**（上流 §4.2 の警告フィールド）。これは 3D で定義されたものだが **2D にも同じだけ有効**なため、**`F-CORE-06` の Core の作法として引き上げた**（FR-7a〜7f）——対象 / 実測値と推奨値と前提 / **その先で何が起きるか** / **直し方とこのアプリで直せるか** / **無視してよい条件** / 検査規則の版。
    - とりわけ **「無視してよい条件」** は、`F-3D-03` FR-11 が置いた課題——**厳しすぎて無視される基準は、基準として機能しない**——への回答になる。**無視してよい条件を先に書いておけば、無視は「規律ある判断」になり、警告全体の信用が保たれる**。
- **F-3D-13 / F-3D-14 / F-3D-15 完了**（`T-DONE-7`/`T-DONE-8` が揃った）。**3D の一続き——取り込み → 検品 → 整備 → 軽量化 → 記録 → 書き出し——が機能単位で通った。** 着手前に `3d/3D_IMPORT_INSPECTION_SETUP_EXPORT_SPEC.md` §5・§8〜§9 を読む運用を継続した。
  - `F-3D-13`（器）: **利用者が申告した利用条件を正とし、埋め込み情報で自動上書きしない・矛盾は警告のみ**（`3D-RISK-05`。**機械が権利を判定しない**）。**未確認を「未確認」として持てる**——**空欄を「制限なし」と読ませない**。`3D-RISK-10` から **optional を積み増すだけにせず、追加のたびに版の扱いを判断する**（積み増しだけでは版管理が一度も試されないまま破綻する）も入れた。
  - `F-3D-14`（記録）: **画面の結果は閉じれば消える**——ゲームへ持ち込む相手・後日の自分に対して証拠を残すのが役割。上流 §5 から **どのバイト列に対する検査かを必ず紐づける**（別のモデルの記録が付いたままにならない）、**追記型で既存を書き換えない**、**最新 1 件は必須・履歴は容量を見て任意**（履歴で保存を膨らませない）を継承した。
  - `F-3D-15`（出口）: 2D と同じ **「対応」を曖昧にしない**に加え、3D 固有の重心として **既定ではモデルに手を加えずに出す**（上流 §8.2）。整備した基準は付随データとして同梱し、**変換は受け取り側の作業として取り込み手順に書く**。焼き込みは既定無効で、**骨・動きを持つモデルでは特に警告**する。
    - **再現可能な出力**（§8.5）が具体的で有用だった: 並び順・時刻・整形を固定し、**毎回変わる値は目録の 1 か所にだけ置き、他のファイルへ埋め込まない**（**あちこちに時刻が入ると、それだけで毎回別物になる**）。**再現できない操作を含む場合は理由を明示**し、**2 回書き出して一致を確かめられる**。
    - `3D-RISK-11` から **対象ごとの制約（骨の数・影響数・命名）を、書き出し前検査・取り込み手順・検品プリセットの三か所が同じ所在から参照する**（別々に書くと必ずずれ、**書き出しは成功するのに対象側で壊れる**）。
    - §8.7 から **一式の中の位置は相対のみ・上位を辿る表記や制御文字を含めない**（`F-3D-01` FR-5 / `F-MOD-02` FR-7b と同じ思想が、今度は**出す側**に現れた）。
- **F-3D-02 完了**（`T-DONE-2`）。着手前に上流 §1〜§3 を読む運用を継続した。
  - **上流との差を初めて明示的に扱った機能**。上流は当初 OBJ を後続候補としていたが、**「材質・アニメーションの表現力が低く、中心形式へ集約する」**として対象外へ移し、再判断項目に残していた。本コンセプトの `T-DONE-2` は他形式の取り込みを含むため、**この判断を制約としては引き継がない**。ただし**判断の理由——表現力の差を抱えたまま「対応」と言うと後で必ず破綻する——は継承した**（`APP_PRODUCT_VISION.md` 6 章の「知見として継承し、制約としては継承しない」の実践）。
  - そのため本機能の中身は **「どの形式に対応するか」ではなく「形式を増やしてよい条件と、増やしたときに守ること」** にした。形式追加の 5 条件（安全な検証を設計できるか / 表現力の差を説明できるか / 解析部品のライセンスを通せるか / 対象環境で現実的に動くか / **検品と書き出しが成立するか**）を満たすまで「対応」と表示しない。**入口だけ作って出口が無い形式を増やさない。**
  - 上流 §3 から特に有用だったのは **拒否と警告の分け方**——**拒否するのは資源の枯渇に直結するものに限り、それ以外は警告を出したうえで表示を試みる**（**判らないものを全部拒否すると、実際の素材が入らなくなる**）。あわせて **未知の塊を捨てず保持して警告する**（`D3` の未知データ非破壊と同じ思想）、**拡張子や宣言種別を主根拠にしない**、**上限は暫定として扱い実測で確定する**を継承した。
  - §2 の **フォルダ操作を必須経路にしない**（一部の端末で使えないため、それしか道が無い状態を作らない）も入れた。
- **🎉 F-3D-08 / F-3D-09 完了 — 3D エディション（着手可能な全 15 機能）が揃った**。`F-3D-01`〜`15` がすべて「詳」になり、**`T-DONE-1`〜`T-DONE-9` のすべてに担当機能が付いた**（`F-3D-16`〜`18` は `APP-O-04` 判断待ちの保留）。着手前に `3d/3D_INTEROP_VRM_VR_AND_CREATION_SPEC.md` §4〜§5 を読む運用を継続した。
  - **`APP-O-04` が未判断のため、宣言どおり現行 `T-DONE-5` の範囲（検査と対応付けまで）で書き、付与は保留のまま据え置いた**。`F-3D-08` の §3.7 に将来の境界を明記してある——付与を採る場合は **骨を足した派生でモデルの構成が変わるため、対応付け・アンカー・判定を派生側の番号へ張り直す必要がある**（`3D-RISK-02` が「実装時の最頻出バグ想定箇所」と名指しした点）、かつ **基準の確定を前提条件にする**（`3D-RISK-12`）。
  - `F-3D-08` の核心は **既存の標準語彙を採用し、独自語彙を作らない**（上流 §4.2）。**独自語彙は、書き出し先での流用も他ツールとの往復もすべて難しくする**。あわせて **名前からの自動推定は「候補の提示」までとし、確定は必ず利用者の操作**（**名前の当てずっぽうで骨の役割を決めない**）、**番号を正・名前は表示と再確認用**（`F-3D-06` FR-12b と同じ宣言点の規則）を継承した。
    - 位置づけとして、**対応付けはそれ自体が目的ではなく、他の機能を賢くするための土台**である——できると `F-3D-06` が「右手に置く」と言えるようになる（ただし**補助であって自動配置ではない**）。
  - `F-3D-09` が扱う形式は、**形式そのものが利用条件を内包している**点で他と違う。多くの 3D モデルは「使ってよいか」がファイルの外にあるが、この形式は**中に書いてある**——`F-3D-13` の「利用条件を記録する」にとって好都合である。ただし線引きが要る: **中に書いてあることを機械が正しいと保証はしない**（上流 §5.2 / ADR-0013 と同じ境界）。**申告が正・自動上書きしない・食い違いは警告のみ・書き出しは素通しのみで利用条件を書き換えた派生を作らない**を継承した。
    - **`F-3D-02` の「未知の塊を捨てない」がそのまま効く**のも収穫だった。拡張データをバイト列のまま保持できるため、**追加の実装なしで素通しが成立する**——安全規律が、別の機能の実現手段になっている。
- **F-UX-01 / F-UX-02 / F-UX-03 完了**。着手前に `3d/3D_UI_UX_SPEC.md` §1・§6〜§8 を読んだ。**機能が出揃ってから横断の作法を抽出する**という順序（先に決めた見た目に機能を合わせるのではない）が、そのまま利いた。
  - `F-UX-01`（一貫性）は **揃えるものと揃えないものを分ける**のが中心。揃えるのは**見た目・言葉遣い・配置の規則・状態表示の形・待たせる処理の作法・履歴経路**。揃えないのは**編集面そのもの**（`P5` の目的分離）。特に `3D-RISK-06` から **語彙が同じでも実装・型・検証を共有しない**（**同じ言葉に見えて完全に別物のものがある**。共有してよいのは呼び名まで）を継承した。
  - `F-UX-02`（到達性）の核心は、上流 §1-1 が最初に置いた原則——**「Canvas だけに情報を依存させない。視覚的に確認できない利用者も、すべての作業を完了できる」**。これは **「あとから読み上げ対応を足す」とは根本的に違い、描画面を情報の唯一の出どころにしないという設計の制約**である。あわせて **代替情報は常設**（流れて消える通知は後から読み返せない）、**色だけで状態を区別しない**、**動きを抑える設定を尊重**、**ドラッグ中の意図しない接触はドラッグ優先**を継承した。
    - **道具と作品で同じ規律が現れた**点も記録した。`F-2D-13`（色の制約）や `F-3D-07`（判定の目的表示）で「色だけに意味を託さない」と決めたのと同じことを、道具自身にも要求している。
  - `F-UX-03`（伝わり方）は、**ここまでの全機能が繰り返し要求してきたもの**——理由つき拒否・loss 開示・復旧手段・未検査の明記——を**人が読んで動ける文章にする**横断要件。`F-CORE-06` が中身を定めたが、**中身が揃っていても伝わらなければ意味がない**。Vision の非採用の法則どおり、**失敗したときに何が起きたか分からない道具は、その一度で見限られる**。
    - 独自に足したのは **警告疲れの制御**（重大度の区別・件数と導線・**無視してよい条件の併記**・同じ指摘を繰り返さない）と、**取り返しがつくかを示す**（原子性を約束した機能では「何も変わっていない」と明言できる）、**推測で原因を断定しない**、**秘密情報をメッセージに含めない**。
- **F-UX-04 / F-UX-05 完了 — UX 横断が揃った**。着手前に `../2D_DEVICE_RELIABILITY_SPEC.md` §3〜§4・§9 を読んだ。
  - `F-UX-04`（対象端末）は **本コンセプト固有の前提の違い**を明示的に扱った。上流はブラウザ版で携帯端末を含めることが必須だったが、**本コンセプトはネイティブアプリであり対象端末の範囲そのものが別判断**。よって**端末一覧は引き継がず、決めたあとに守る規律を継承**した——とりわけ上流 §9 が言い切った **「単に画面が開くことでは合格にしない」**。あわせて **編集能力と到達能力を分ける**（すべての環境に同じ快適さを約束しない代わりに、**行き止まりは作らない**）、**できないこと・向かないことを明示**（黙って遅くするより先に言う）、**精密操作は拡大・つまみ・数値入力の複数手段**、**使えない入力があっても同じ結果へ到達できる**（**道具の差が成果の差にならない**）、**自動の確認だけを根拠にしない**（自動は退行防止、人手は操作感——役割を分ける）を継承した。
  - `F-UX-05`（初回起動）は、**本コンセプトの主張が最初に試される場所**。Vision は「重すぎるなら使われない」と置き、Architecture は「必要な機能だけを入れる」と答えたが、**初回起動はその答えが裏目に出やすい場面**でもある——**最小構成で起動するとは、最初は何も入っていないということ**。ここで「何もできない」と感じさせたら、install-on-demand は**利点ではなく障害**になる。
    - そこで要を **「軽く起動すること」と「すぐ何かできること」の両立**に置いた。**最小構成のまま作成 → 編集 → 書き出しが通る**（「まず何か入れないと始まらない」状態にしない）、**できることから示す**（できないことの一覧から始めない）、**説明の踏破を強制せずあとから見返せる**、**勧誘で画面を散らかさない**、**勝手に導入しない**、**示すことと入れることを分ける**。
- **🎉 F-MOD-05 / F-MOD-06 完了 — 全 53 機能のインベントリが埋まった**（`F-3D-16`〜`18` は `APP-O-04` 判断待ちの保留 3 件）。**「未」は 0 件**。
  - `F-MOD-01`〜`04` は install-on-demand を **機構として**完成させた（一覧でき、入れられ、依存が解け、ライセンスが検証される）。しかし **機構が正しくても、利用者が「自分に何が必要か」を判断できなければ、選択は負担にしかならない**。**選択肢の多さは、それ自体が使われない理由になり得る**（`P1`）——**多機能ゆえに重いアプリを避けた先で、今度は選択の重さに直面する**。この 2 機能はその橋を架ける。
  - `F-MOD-05`（束ねる）の生命線は **提示と決定を分ける**こと。**「おすすめ」を既定で入れてしまえば、それはただの全部入り**であり、install-on-demand の原則を裏切る。よって **提案であって既定ではない**・**適用は明示的な操作**・**適用前に中身と合計の重さを示す**・**一部だけ選べる（束は分解できる）**・**断っても不利益がなく繰り返し勧めない**を核に据えた。さらに **最小構成も一つの選択肢として並べる**（**何も足さないことが正当な選択である**と示す）、**束の中に「とりあえず」を作らない**、**束ねることで検証を迂回させない**を入れた。
  - `F-MOD-06`（その時に示す）は **install-on-demand の最後の一片であり、最も難しい一片**。本当に必要な機能は**先には分からず、やろうとした瞬間に分かる**。そのとき **何も起きないのが最悪**で、**機能が無いことに気づけないまま「このアプリではできない」と誤解される**——**気づけなければ存在しないのと同じ**。一方で**やりすぎれば勧誘だらけの画面**になる（`F-MOD-01` FR-6 / `F-CORE-08` FR-10 が見ていた危険）。よって要件を **この二つの失敗——気づけない / うるさい——の間に細い道を通すこと**に定めた。
    - 最重要の区別は **「できない」と「入れれば できる」**。加えて **何ができるようになるかを示す**（名前だけでは自分に要るか分からない）、**代替手段があれば併せて示す**（**入れさせることが目的ではない**）、**一度断った提示を繰り返さない**が効く。
    - **作業内容を外部へ送らない**（オフライン前提。**何を作っているかで推薦しない**）も明記した。

---

## 8. 到達点（2026-07-26 時点）

**全 53 機能の詳細化が完了した。**

| 領域 | 機能数 | 状態 |
|---|---:|---|
| Core / 共有土台（`F-CORE-01`〜`10`） | 10 | **詳** |
| モジュール機構（`F-MOD-01`〜`06`） | 6 | **詳** |
| プラットフォーム・性能（`F-PLT-01`〜`05`） | 5 | **詳** |
| 2D Edition（`F-2D-01`〜`16`） | 16 | **詳** |
| 3D Edition（`F-3D-01`〜`15`） | 15 | **詳** |
| UX / 横断（`F-UX-01`〜`05`） | 5 | **詳** |
| 3D の付与（`F-3D-16`〜`18`） | 3 | **保留**（`APP-O-04` 判断待ち） |

完成系の充足:

- **`D-DONE-1`〜`8`（2D）**: すべてに担当機能が付いた。
- **`T-DONE-1`〜`9`（3D）**: すべてに担当機能が付いた。
- **`S-DONE-1`〜`5`（共有）**: すべてに担当機能が付いた。

**残る作業**:

1. **人間判断**: `APP-O-04`（骨組みの当てはめ・肌の自動追従・動きの付与を 3D の範囲に含めるか）。判断次第で `F-3D-08` の射程と `F-3D-16`〜`18` の扱いが決まる。
2. **上流 3D 一式の未突き合わせ範囲**（`3D_ASSET_DATA_CONTRACT.md` / `3D_ARCHITECTURE_AND_BOUNDARIES.md` / `3D_PERFORMANCE_DEVICE_SECURITY_LICENSE_SPEC.md` ほか）。既存機能の再確認として順次。
3. **統合文書**（`APP_CORE_CONTRACT.md` / `APP_DATA_CONTRACT.md` / `APP_2D_EDITION_SPEC.md` / `APP_3D_EDITION_SPEC.md` / `APP_MODULE_MANIFEST_SPEC.md` / `APP_PERFORMANCE_AND_SHELL.md`）。関係マップ 5 章のカバレッジ表が queue。**機能単位の要件は揃ったので、次はそれらを層ごとに束ねる段**になる。
4. **Concept Gate**（`APP_ROADMAP_DECISIONS_OPEN_ITEMS.md`）。実装着手には人間承認が要る。
- その後: UX 横断（`F-UX-01`〜05）→ `F-MOD-05/06`（プロファイル / 文脈連動提示）。
- **注意（継続）**: **2D Pro Gate の人間承認前は、3D の実装・library 評価・dependency 追加を開始しない**（`CLAUDE.md` 2 章）。本 `features/` は docs-only の要件洗い出しでありその禁止事項には当たらないが、**外部ライブラリの具体名を採用として書かない**。
